# M1W2D1 学习与代码计划（第2周）

本周主题与 M1W1 一致，在 W1 基础上**深化与练习**。

## 对照

- 同日主题见 **M1/W1/M1W1D1** 的 `plan.md`（周一 · LLVM/C++）。

## 本日建议

- 复习：Module/Function/BasicBlock、isa/cast/dyn_cast、IRBuilder、Phi。
- 深化：写一个完整小 Pass（或独立工具）遍历 Module 内所有 Function，统计各指令类型数量；或生成带循环的 IR（如 sum 0..n）并跑通。
- 代码：放在 `lib/` 与 `tools/`，测试放在 `test/`。
