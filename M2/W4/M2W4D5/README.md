# M2W4D5 · 第4周 周五 · Python 与性能

核心期第 4 周第 5 天，M2 收尾。对照 M2/W1/M2W1D5。可做本月小结、测试整合，与 M3（实战期）衔接。

## 在 Cursor 中执行

```bash
cmake -B build && cmake --build build
./build/daytool
ctest --test-dir build
```
