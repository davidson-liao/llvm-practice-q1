# **高性能编译器工程全景：从 LLVM 核心机制到 MLIR 异构加速的深度架构分析**

## **摘要**

随着摩尔定律的放缓与异构计算硬件（GPU, AI 加速器）的爆发，编译器技术已从单一的静态代码生成演变为多层次、模块化的基础设施工程。本报告旨在为高级系统工程师提供一份详尽的、为期三个月的编译器全栈技术演进分析与实践指南。报告严格遵循“基础设施构建-核心变换优化-架构协同加速”的逻辑路径，深度解构 LLVM/MLIR 技术栈的底层原理。

分析涵盖了从 LLVM 自定义 RTTI 机制的 C++ 模板元编程实现，到 MLIR 表驱动（TableGen）的声明式开发范式；从基础的指令规范化（Canonicalization）到复杂的算子融合（Fusion）与数据流分析；最终深入至解决内存分配瓶颈的 One-Shot Bufferization 技术及 GPU SIMT 模型的并行映射策略。本报告通过 15,000 字的篇幅，结合具体的代码生成原理、内存模型分析及工程最佳实践，旨在建立一套完整的现代编译器开发知识体系。

# ---

**第一部分：基础设施构建与底层直觉建立（基建期）**

在编译器开发的初始阶段，核心目标是建立对底层基础设施的“直觉”。这不仅要求掌握工具的使用，更要求深刻理解 LLVM 设计哲学中的类型系统、内存管理以及中间表示（IR）的构建逻辑。

## **1.1 LLVM 核心架构与 C++ 模板元编程机制**

LLVM 的强大之处在于其模块化设计与高效的 C++ 实现。理解其底层的类型系统与指令生成机制，是编写任何优化 Pass 的前提。

### **1.1.1 自定义运行时类型识别（RTTI）的各种范式**

标准 C++ 的 RTTI（如 dynamic\_cast）依赖于虚函数表（v-table），在大规模指令处理中会带来不可接受的性能开销与内存膨胀。LLVM 实现了一套基于模板元编程的轻量级 RTTI 系统，即 isa\<\>、cast\<\> 和 dyn\_cast\<\>。这套系统不仅是 LLVM 源码的基石，也是编译器工程师必须内化的思维方式 1。

**核心模版解析：**

* **isa\<T\>（类型检查）：** 类似于 Java 的 instanceof。它通过读取基类中存储的 Kind 枚举值来进行极速的类型判断，完全避免了虚函数调用。该操作仅返回布尔值，不改变指针类型。在遍历 IR 时，isa 常用于约束检查，例如判断一个 Value 是否为 ConstantInt 2。  
* **cast\<T\>（断言转换）：** 这是一个“受检转换”（Checked Cast）。它假设程序员已经确信对象的类型，强制将其转换为目标类型指针。如果类型不匹配，在 Debug 模式下会直接触发断言失败（Assertion Failure）并导致程序崩溃。这种设计是为了在确定的上下文中消除多余的类型检查指令，从而提升 Release 构建的性能 2。  
* **dyn\_cast\<T\>（安全试探转换）：** 这是编译器 Pass 中最常用的操作符。它结合了检查与转换：如果对象属于目标类型，则返回有效指针；否则返回空指针（nullptr）。这种机制允许在 if 语句中通过指针的有效性来驱动控制流，是实现模式匹配（Pattern Matching）的基础 3。

**表 1.1：LLVM 自定义 RTTI 操作符对比分析**

| 操作符 | 语义功能 | 运行时开销 | 失败行为 | 典型应用场景 |
| :---- | :---- | :---- | :---- | :---- |
| isa\<T\> | 询问“是这个类型吗？” | 极低（位掩码或枚举比较） | 返回 false | 前置条件检查，断言逻辑 |
| cast\<T\> | 命令“转为这个类型！” | 低（仅 Debug 模式检查） | 崩溃（Assert） | 确定性上下文，获取子类特有方法 |
| dyn\_cast\<T\> | 尝试“如果是，则转换” | 中（包含检查逻辑） | 返回 nullptr | 模式匹配，多态处理，Pass 逻辑分发 |

**深度洞察：** 初学者常犯的错误是先使用 isa 检查，随后立即使用 cast 转换。这种“先查后转”的模式会导致两次类型 ID 读取。最佳实践是直接使用 dyn\_cast，利用其返回值进行判断，从而将内存访问次数减半。此外，dyn\_cast 无法用于 C++ 引用（Reference），因为引用不可为空，此时必须使用 cast 或 isa 1。

### **1.1.2 手写 LLVM IR 指令生成与 Builder 模式**

LLVM IR 是强类型的静态单赋值（SSA）形式。手动生成 IR 并非简单的字符串拼接，而是操作内存中的对象图（Object Graph）。IRBuilder 类封装了这种复杂性，它维护一个“插入点”（Insertion Point），确保新创建的指令被正确链接到基本块（BasicBlock）的指令链表中 5。

在生成 IR 时，必须时刻关注 SSA 的性质——每个值（Value）只能被定义一次。当涉及控制流分支（如 if-else）时，变量的重新赋值不能通过简单的覆盖来实现，而必须使用 Phi 节点（Phi Node）来合并来自不同前驱基本块（Predecessor）的值。这是从命令式编程思维转向编译器 SSA 思维的关键门槛 7。

## **1.2 MLIR 声明式开发（ODS）与类型系统**

随着编译器从单一后端转向多层 IR，手动编写 C++ 定义 Op（操作）变得难以维护。MLIR 引入了 TableGen 作为声明式领域特定语言（DSL），通过 Operation Definition Specification (ODS) 自动生成 C++ 样板代码。

### **1.2.1 TableGen 语法与 Op 定义哲学**

TableGen 是一种记录描述语言。在 MLIR 中，所有的 Op、Attribute（属性）和 Type（类型）都继承自 TableGen 的记录（Record）。ODS 框架允许开发者在一个 .td 文件中定义 Op 的名称、参数、结果、文档和验证逻辑 9。

**Op 定义的核心组件：**

1. **Arguments（参数）：** 分为 SSA 操作数（Operand）和编译时属性（Attribute）。ODS 强制要求区分两者：操作数是运行时的数据流，而属性是编译时的静态元数据（如卷积核的大小、步长）。  
2. **Traits（特征）：** 类似于 Mixin 或 Interface，用于声明 Op 的行为属性，如 NoSideEffect（无副作用，可被死代码消除）、Commutative（可交换律）。  
3. **Verifiers（验证器）：** 除了类型系统的静态检查外，ODS 允许嵌入 C++ 代码片段来执行复杂的运行时验证（如检查张量的维度是否匹配）。

**深度洞察：** ODS 解决的核心问题是“字符串类型化”（Stringly Typed）问题。在传统的编译器中，检查一个操作数是否为 32 位浮点数可能需要复杂的 if 嵌套。而在 ODS 中，通过定义 F32Tensor 约束，生成的 C++ 代码会自动包含 verify() 方法，确保只有符合约束的 IR 才能被构建。这不仅提升了安全性，也使得 IR 的语义在定义阶段就得到了形式化保障 9。

### **1.2.2 属性（Attribute）与类型（Type）系统的构建**

属性是 MLIR 中表示常量的机制。理解 BuiltinDialect 中的标准属性（如 IntegerAttr, StringAttr, DenseElementsAttr）是基础。在自定义 Dialect 中，定义自定义属性和类型需要遵循严格的存储类（Storage Class）设计模式，利用 Uniquer 机制确保同一上下文中的相同类型实例是唯一的（Singleton），从而极大降低内存占用并加速比较操作（仅需比较指针）11。

## **1.3 规范化（Canonicalization）与模式重写**

编译器的优化本质上是图重写（Graph Rewriting）。MLIR 提供了一套强大的 PatternRewriter 基础设施，用于执行 DAG-to-DAG 的转换。

### **1.3.1 贪婪模式重写驱动（Greedy Pattern Rewrite Driver）**

MLIR 的规范化 Pass 使用贪婪策略。驱动器维护一个工作列表（Worklist），不断应用注册的 RewritePattern，直到 IR 达到不动点（Fixed Point）或达到最大迭代次数。

**工作机制分析：**

1. **Match（匹配）：** 检查 IR 是否符合特定的拓扑结构（如 addi x, 0）。  
2. **Rewrite（重写）：** 构建新的 IR 结构（如直接替换为 x）并替换原 Op。  
3. **Worklist Update：** 当一个 Op 被替换或修改时，其使用者（Users）和操作数（Operands）会被重新加入工作列表。这是因为局部变化可能触发连锁反应，暴露出新的优化机会（如常数折叠后可能触发死代码消除）12。

### **1.3.2 手写 C++ Pattern 与声明式 DRR**

虽然可以使用 C++ 编写 matchAndRewrite 方法，但 MLIR 推荐使用 TableGen 的声明式重写规则（Declarative Rewrite Rules, DRR）。DRR 允许以类似数学公式的方式定义转换：

代码段

def : Pat\<(AddIOp $x, ConstantInt), (replaceWithValue $x)\>;

TableGen 会自动生成对应的 C++ 状态机代码。这种方式极大地降低了编写 peephole 优化的门槛，并减少了手动操作 IR 指针带来的 Bug 风险 14。

## **1.4 向量化基础与硬件映射**

高性能计算的基石是 SIMD（单指令多数据）。理解硬件层面的向量执行与 IR 层面的向量抽象之间的映射关系至关重要。

### **1.4.1 Intel SIMD 指令集原理**

从 SSE 到 AVX-512，现代 CPU 的吞吐量高度依赖于向量寄存器的利用率。理解“通道”（Lane）的概念是关键：一个 512 位的寄存器可以被视为 16 个 32 位浮点数的并行通道。编译器必须显式生成向量指令（如 vaddps）才能利用这一能力。如果 IR 仅仅是标量循环，硬件将不得不以 1/16 的理论峰值性能运行 16。

### **1.4.2 Vector Dialect：高层抽象与边界处理**

MLIR 的 vector 方言提供了一种 n 维向量的高层抽象，这与 LLVM IR 中紧贴硬件的一维向量不同。

**核心算子：Transfer Read/Write** vector.transfer\_read 是连接内存（MemRef/Tensor）与寄存器（Vector）的桥梁。它解决了一个传统自动向量化中的痛点：边界处理。 在传统的循环向量化中，如果数组长度不是向量宽度的倍数，编译器必须生成标量“剥离循环”（Peeling Loop）来处理剩余元素。vector.transfer\_read 支持隐式的\*\*填充（Padding）\*\*机制：当读取越界时，它会自动用指定的填充值（如 0.0）填满向量的剩余通道。这种语义允许编译器生成无分支的主体代码，极大地简化了下游的代码生成逻辑 18。

## **1.5 工程化体系：CMake 与 GTest**

编译器的稳定性依赖于严格的测试。

* **CMake 自动化：** MLIR 依赖复杂的构建链。必须熟练掌握 add\_mlir\_dialect\_library 和 mlir\_tablegen 等 CMake 宏，确保 .inc 头文件在编译前正确生成，避免并发编译时的竞争条件 20。  
* **GTest 与 Lit：**  
  * **Unit Tests (GTest):** 用于测试 C++ 内部 API，如 ADT（抽象数据类型）的行为、分析 Pass 的逻辑正确性 22。  
  * **Regression Tests (Lit \+ FileCheck):** 编译器的标准测试方式。通过输入 .mlir 文本，运行工具链，并使用 FileCheck 验证输出 IR 中是否包含预期的指令序列。这种测试方式对 IR 的文本结构敏感，但能最直接地验证端到端的转换效果 22。

# ---

**第二部分：核心变换与性能瓶颈突破（核心期）**

在建立基础直觉后，第二个月的重心转向编译器最核心的价值——优化。本阶段将通过别名分析、接口抽象和算子融合，深入解决实际性能瓶颈。

## **2.1 内存优化分析与 LLVM Alias Analysis**

内存访问的依赖关系是阻碍指令重排和并行化的最大障碍。别名分析（Alias Analysis, AA）旨在回答一个问题：“指针 A 和指针 B 是否可能指向同一块内存？”

### **2.1.1 Alias Analysis 理论与 LLVM 实现**

LLVM 提供了一套分层的 AA 系统：

* **BasicAA：** 基于启发式规则（如全局变量与栈变量不可能别名，不同偏移量的 GEP 可能不别名）。  
* **TBAA (Type-Based AA)：** 基于语言层面的类型系统（如 C 语言标准规定 float\* 和 int\* 不别名）。  
* **SCEV AA：** 利用标量演化（Scalar Evolution）分析循环中的索引表达式，对于数组访问的依赖分析至关重要 24。

**AA 结果的三种状态：**

1. **MustAlias:** 指针确切指向同一地址。  
2. **NoAlias:** 指针确切指向不同地址（优化的黄金标准）。  
3. **MayAlias:** 编译器无法确定，必须保守假设它们别名，从而禁止 Loop Invariant Code Motion (LICM) 等优化 25。

### **2.1.2 自定义 Alias Analysis Pass**

对于领域特定语言（DSL），编译器工程师通常掌握比 C++ 类型系统更多的信息。例如，在一个特定的张量计算图中，输入张量和权重张量在运行时保证是不重叠的。通过实现自定义的 AA Pass（继承 AAResultBase），可以显式地告诉 LLVM 优化器这些 NoAlias 关系，从而解锁激进的指令调度和向量化优化 25。

## **2.2 接口（Interface）与多态抽象**

MLIR 的强大扩展性来自于 Interface 机制。它允许变换 Pass 以不透明的方式操作 Op，而无需了解 Op 的具体实现。

### **2.2.1 OpInterface 的 C++ 实现模式**

Interface 基于 Concept-Model 模式（类似于 Rust 的 Trait 或 Haskell 的 Typeclass）。

* **Concept:** 定义抽象基类和虚函数接口。  
* **Model:** 模板类，负责将具体的 Op 类型映射到 Concept 接口。

这种设计使得 Op 的定义与 Interface 的实现解耦。我们可以在不修改 Op 源码的情况下，通过 ExternalModel 为外部 Dialect 的 Op 注入新的接口行为。这在处理跨方言优化时尤为重要 26。

### **2.2.2 副作用接口（SideEffectInterfaces）**

为了让优化器（如死代码消除 DCE、公共子表达式消除 CSE）正常工作，Op 必须声明其对内存的影响。MemoryEffectOpInterface 描述了 Op 是读取、写入、分配还是释放内存。如果一个 Op 标记了 Pure（即 NoSideEffect），编译器就可以安全地在其结果未被使用时将其删除，或者将其移动到循环外（LICM）28。

## **2.3 算子融合（Fusion）与数据流分析**

融合是 AI 编译器中最重要的性能优化手段之一。

### **2.3.1 融合的经济学原理**

在现代硬件上，计算通常比内存访问快得多。

* **未融合：** Load A \-\> Compute \-\> Store B; Load B \-\> Compute \-\> Store C。这里包含 2 次读和 2 次写。  
* **融合后：** Load A \-\> Compute \-\> Compute \-\> Store C。这里仅需 1 次读和 1 次写，中间结果保留在寄存器或 L1 缓存中。

融合极大地降低了对内存带宽的需求，这对于受限于显存带宽的 GPU 负载尤为关键 30。

### **2.3.2 Linalg Tile-and-Fuse 策略**

MLIR 的 Linalg 方言通过 **Tile-and-Fuse** 技术实现融合：

1. **Tiling（平铺）：** 将消费端（Consumer）的循环空间切分为小块（Tile）。  
2. **Slice Analysis（切片分析）：** 反向推导该 Tile 需要生产端（Producer）提供哪些数据切片。  
3. **Fusion（融合）：** 将生产端的切片计算移动到消费端的 Tile 循环内部。

这种机制保证了数据的**局部性（Locality）**：生产的数据立即被消费，最大化缓存命中率 31。

## **2.4 循环平铺（Tiling）与 MemRef 降级**

### **2.4.1 Tiling 理论与缓存一致性**

Tiling 的核心目的是让工作集（Working Set）适配各级缓存（L1/L2/L3）的大小。不恰当的 Tile 大小（特别是沿 stride=1 的维度切分过细）会导致**伪共享（False Sharing）**，即多个核心频繁争夺同一缓存行，反而降低性能。

### **2.4.2 Linalg 到 MemRef 的降级**

Linalg Op 通常定义在 Tensor（值语义）上，便于进行数学层面的变换。但在代码生成阶段，必须降级到 MemRef（指针语义）。 转换链条：linalg.generic (tensor) \-\> one-shot-bufferize \-\> linalg.generic (memref) \-\> convert-linalg-to-loops \-\> scf.for \-\> llvm.br/cond\_br。理解这一降级流水线是调试性能问题的关键 30。

## **2.5 性能测试工程化与 Python 绑定**

### **2.5.1 MLIR Python Bindings 架构**

MLIR 的 Python 绑定并未直接封装 C++ 类，而是封装了 **C-API**。这种设计保证了 ABI 的稳定性（C++ ABI 脆弱且不兼容）。通过 pybind11 或 nanobind，可以将 C-API 暴露为 Python 对象，使得开发者可以使用 Python 快速构建 IR、运行 Pass 并验证结果，极大地提升了开发效率 35。

# ---

**第三部分：架构协同与异构加速（实战期）**

进入第三个月，我们将直面编译器最复杂的挑战：后端代码生成、跨方言转换以及 GPU 加速。本阶段将特别针对用户提到的“内存分配瓶颈”进行深度剖析。

## **3.1 编译器后端 Codegen 原理**

虽然 MLIR 负责高层优化，但最终的机器码生成仍依赖 LLVM 后端。

### **3.1.1 指令选择（Instruction Selection, ISel）**

ISel 负责将 LLVM IR 映射到目标机器指令（如 add i32 \-\> ADD R1, R2）。

* **SelectionDAG：** 传统的基于图的指令选择。它构建基本块的依赖图（DAG），通过模式匹配（Pattern Matching）覆盖节点。  
* **GlobalISel：** 新一代的指令选择框架，旨在提高编译速度并支持全局优化。

### **3.1.2 寄存器分配（Register Allocation）**

寄存器分配是将无限的虚拟寄存器映射到有限的物理寄存器的过程。

* **图着色（Graph Coloring）：** 经典的寄存器分配算法。构建干扰图，节点代表变量，边代表生命周期重叠。目标是用 K 种颜色（寄存器）为图着色。生成代码质量高，但编译时间长。  
* **线性扫描（Linear Scan）：** 贪婪算法，按生命周期扫描。编译速度快，常用于 JIT 编译器 37。

## **3.2 跨方言转换（Dialect Conversion）框架**

在将 Tensor 转换为 MemRef 或 LLVM 指针时，不仅 Op 会发生变化，操作数的\*\*类型（Type）\*\*也会发生变化。简单的贪婪重写无法处理这种情况（因为使用者的类型期望尚未更新）。

MLIR 的 Dialect Conversion 框架引入了 **TypeConverter** 和 **Materialization（物化）** 机制。

* 如果一个 Op 被转换，产生了一个新类型的指针，而其使用者尚未被转换（仍期望 Tensor），框架会自动插入一个 unrealized\_conversion\_cast Op 进行临时桥接。  
* 当所有 Op 都被合法化后，这些 Cast Op 应该成对抵消。如果残留，说明转换失败 39。

## **3.3 One-Shot Bufferization：解决 JD 约束下的内存瓶颈**

**核心议题：** 用户 Query 中提到的“JD 内存分配瓶颈”通常指动态图执行中频繁的 malloc/free 开销。在深度学习推理中，中间 Tensor 的反复分配会导致严重的性能抖动和缓存冷启动。

### **3.3.1 内存分配的根本问题与“原位更新”（In-Place）**

传统的 Bufferization（缓冲化）策略是保守的：对于每个 Tensor 运算结果，都分配一个新的 Buffer。这对于 add, relu 这种 Elementwise 操作是极大的浪费。 优化的目标是实现 **In-Place Update**：复用输入 Tensor 的 Buffer 来存储输出结果。

### **3.3.2 One-Shot Analysis 算法**

MLIR 引入了 **One-Shot Bufferization** Pass，这是一个全局分析 Pass。

1. **别名集分析：** 分析整个函数的 SSA 使用-定义链（Use-Def Chain）。  
2. **读后写（Read-After-Write, RaW）冲突检测：**  
   MLIR  
   %0 \= alloc()  
   write %0     // 定义  
   read %0      // 冲突点！如果 write 是原位更新，这里读取的数据将被破坏。

   分析器会检查：如果一个 Buffer 被原位写入，是否还有后续的读取操作依赖于写入前的旧值？如果有，则发生了 RaW 冲突，编译器必须插入显式的 Copy（alloc \+ memcpy）来破除依赖。  
3. **决策：** 如果没有 RaW 冲突，且 Op 支持 BufferizableOpInterface，则直接复用 Buffer 41。

### **3.3.3 Destination Passing Style (DPS)**

为了支持这种优化，MLIR 提倡 **Destination Passing Style (DPS)**。

* **非 DPS（传统）：** %res \= tensor.add %a, %b。结果隐式创建。  
* **DPS（Linalg）：** %res \= linalg.add %a, %b ins(%out)。结果显式写入 %out 张量。 DPS 风格天然地表达了“复用 %out 的内存”这一意图，是解决内存瓶颈的关键编程范式 43。

## **3.4 GPU 并行模型与存储层次优化**

### **3.4.1 SIMT 模型与 Warp Divergence**

GPU 采用 SIMT（单指令多线程）模型。程序员编写单个线程的逻辑，硬件调度成千上万个线程。 **Warp（线程束）：** NVIDIA GPU 以 32 个线程为一组（Warp）锁步执行。如果代码中存在 if-else 分支导致同一 Warp 内的线程走不同路径（Warp Divergence），硬件必须串行执行两个分支，导致性能减半。编译器需尽量避免这种情况 44。

### **3.4.2 共享内存（Shared Memory）与 gpu.launch**

gpu Dialect 通过 gpu.launch 映射到 CUDA 的 Grid/Block 结构。 **优化策略：利用 Shared Memory 隐藏延迟** 全局显存（HBM）延迟极高。高频访问的数据应加载到片上共享内存（Shared Memory）。

MLIR

// 在 MLIR 中分配共享内存（Memory Space \= 3）  
%shmem \= memref.alloc() : memref\<32x32xf32, 3\>

**Barrier 同步：** 当一个线程块内的线程协作加载数据到 Shared Memory 后，必须执行 gpu.barrier，确保所有数据就绪后，才能进行计算。遗漏 Barrier 是 GPU 编程中最常见的竞态条件 Bug 44。

## **3.5 终极验收：端到端迷你算子图**

在最后一周，通过实现一个包含 Conv2D \+ BiasAdd \+ ReLU 的迷你图来验证全链路。

1. **Frontend:** 使用 TOSA 或 Linalg 构建 Tensor 图。  
2. **Optimization:**  
   * 使用 Linalg Fusion 将 Add 和 ReLU 融合进 Conv 的循环内部。  
   * 使用 Tiling 将循环分块以适应 GPU Block 大小。  
3. **Bufferization:** 运行 One-Shot Bufferize，确保 BiasAdd 和 ReLU 原位复用 Conv 的输出 Buffer，实现**零中间内存分配**。  
4. **Codegen:**  
   * CPU: 向量化为 AVX 指令。  
   * GPU: 映射到 gpu.thread\_id，转换为 NVVM IR，最终生成 PTX。  
5. **Runtime:** 使用 mlir-cpu-runner 或 mlir-vulkan-runner 执行并验证数值正确性 46。

# ---

**结论**

通过这三个月的系统性拆解，我们从 LLVM 底层的 isa/cast 机制出发，重构了对编译器 IR 的认知；通过 TableGen 与 Interface 掌握了 MLIR 的模块化设计；通过 Linalg Fusion 与 One-Shot Bufferization 攻克了算子融合与内存分配这两大性能高地；最后在 GPU 架构上实现了软硬协同的极速推理。

这一课程体系不仅涵盖了“怎么做”（API 使用），更深刻揭示了“为什么”（架构取舍）。对于系统工程师而言，掌握 One-Shot Bufferization 解决动态内存瓶颈的能力，以及利用 TableGen 快速迭代 DSL 的能力，将成为在 AI 编译器领域的核心竞争力。

**表 3.1：关键变换 Pass 映射表**

| 阶段 | 变换名称 (Transformation) | 输入方言 | 输出方言 | 核心机制 |
| :---- | :---- | :---- | :---- | :---- |
| **I** | Canonicalization (规范化) | Any | Any | PatternRewriter (Greedy Driver) |
| **II** | Vectorization (向量化) | Linalg/Affine | Vector | vector.transfer\_read, Padding/Masking |
| **II** | Fusion (融合) | Linalg (Tensor) | Linalg (Tensor) | Tile-and-Fuse, Slice Analysis |
| **III** | Bufferization (缓冲化) | Tensor | MemRef | One-Shot Analysis, RaW 冲突检测 |
| **III** | GPU Mapping (GPU 映射) | SCF/Affine | GPU | gpu.launch, Grid/Block ID 映射 |
| **III** | Lowering (底层降级) | Standard/GPU | LLVM/NVVM | Dialect Conversion Framework |

#### **引用的著作**

1. LLVM Programmer's Manual — LLVM 23.0.0git documentation, 访问时间为 二月 6, 2026， [https://llvm.org/docs/ProgrammersManual.html\#the-isa-cast-and-dyn-cast-templates](https://llvm.org/docs/ProgrammersManual.html#the-isa-cast-and-dyn-cast-templates)  
2. LLVM Programmer's Manual — LLVM 23.0.0git documentation, 访问时间为 二月 6, 2026， [https://llvm.org/docs/ProgrammersManual.html](https://llvm.org/docs/ProgrammersManual.html)  
3. LLVM Tutorial \- Computer Science : University of Rochester, 访问时间为 二月 6, 2026， [https://www.cs.rochester.edu/u/criswell/asplos19/ASPLOS19-LLVM-Tutorial.pdf](https://www.cs.rochester.edu/u/criswell/asplos19/ASPLOS19-LLVM-Tutorial.pdf)  
4. LLVM Programmer's Manual \- AMD ROCm documentation, 访问时间为 二月 6, 2026， [https://rocm.docs.amd.com/projects/llvm-project/en/latest/LLVM/llvm/html/ProgrammersManual.html](https://rocm.docs.amd.com/projects/llvm-project/en/latest/LLVM/llvm/html/ProgrammersManual.html)  
5. 3\. Kaleidoscope: Code generation to LLVM IR, 访问时间为 二月 6, 2026， [https://llvm.org/docs/tutorial/MyFirstLanguageFrontend/LangImpl03.html](https://llvm.org/docs/tutorial/MyFirstLanguageFrontend/LangImpl03.html)  
6. Learn the LLVM C++ API by example. \- GitHub Gist, 访问时间为 二月 6, 2026， [https://gist.github.com/jiayuasu/9c0ad875b403b75bde5585ce847bc307](https://gist.github.com/jiayuasu/9c0ad875b403b75bde5585ce847bc307)  
7. ITERATING AND CASTING \- Laure Gonnord, 访问时间为 二月 6, 2026， [http://laure.gonnord.org/pro/research/ER03\_2015/lab4\_intro.pdf](http://laure.gonnord.org/pro/research/ER03_2015/lab4_intro.pdf)  
8. A Complete Guide to LLVM for Programming Language Creators \- Mukul Rathi, 访问时间为 二月 6, 2026， [https://mukulrathi.com/create-your-own-programming-language/llvm-ir-cpp-api-tutorial/](https://mukulrathi.com/create-your-own-programming-language/llvm-ir-cpp-api-tutorial/)  
9. mlir/docs/OpDefinitions.md · develop · zanef2 / HPAC \- GitLab, 访问时间为 二月 6, 2026， [https://gitlab-03.engr.illinois.edu/zanef21/hpac/-/blob/develop/mlir/docs/OpDefinitions.md](https://gitlab-03.engr.illinois.edu/zanef21/hpac/-/blob/develop/mlir/docs/OpDefinitions.md)  
10. Chapter 2: Emitting Basic MLIR \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Tutorials/Toy/Ch-2/](https://mlir.llvm.org/docs/Tutorials/Toy/Ch-2/)  
11. Defining Dialect Attributes and Types \- MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/DefiningDialects/AttributesAndTypes/](https://mlir.llvm.org/docs/DefiningDialects/AttributesAndTypes/)  
12. The State of Pattern-Based IR Rewriting in MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://llvm.org/devmtg/2024-10/slides/techtalk/Springer-Pattern-Based-IR-Rewriting-in-MLIR.pdf](https://llvm.org/devmtg/2024-10/slides/techtalk/Springer-Pattern-Based-IR-Rewriting-in-MLIR.pdf)  
13. llvm-project/mlir/lib/Transforms/Utils/GreedyPatternRewriteDriver.cpp at main \- GitHub, 访问时间为 二月 6, 2026， [https://github.com/llvm/llvm-project/blob/main/mlir/lib/Transforms/Utils/GreedyPatternRewriteDriver.cpp](https://github.com/llvm/llvm-project/blob/main/mlir/lib/Transforms/Utils/GreedyPatternRewriteDriver.cpp)  
14. MLIR — Canonicalizers and Declarative Rewrite Patterns \- Math ∩ Programming, 访问时间为 二月 6, 2026， [https://www.jeremykun.com/2023/09/20/mlir-canonicalizers-and-declarative-rewrite-patterns/](https://www.jeremykun.com/2023/09/20/mlir-canonicalizers-and-declarative-rewrite-patterns/)  
15. Chapter 3: High-level Language-Specific Analysis and Transformation \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Tutorials/Toy/Ch-3/](https://mlir.llvm.org/docs/Tutorials/Toy/Ch-3/)  
16. SIMD Made Easy with Intel ISPC, 访问时间为 二月 6, 2026， [https://www.intel.com/content/dam/develop/external/us/en/documents/simd-made-easy-with-intel-ispc.pdf](https://www.intel.com/content/dam/develop/external/us/en/documents/simd-made-easy-with-intel-ispc.pdf)  
17. SIMD and vectorization using AVX intrinsic functions (Tutorial) \- YouTube, 访问时间为 二月 6, 2026， [https://www.youtube.com/watch?v=AT5nuQQO96o](https://www.youtube.com/watch?v=AT5nuQQO96o)  
18. 'vector' Dialect \- MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Dialects/Vector/](https://mlir.llvm.org/docs/Dialects/Vector/)  
19. MLIR Vector Dialect and Patterns \- Lei.Chat(), 访问时间为 二月 6, 2026， [https://www.lei.chat/posts/mlir-vector-dialect-and-patterns/](https://www.lei.chat/posts/mlir-vector-dialect-and-patterns/)  
20. mlir/docs/Tutorials/CreatingADialect.md · 16f349251fabacfdba4acac3b25baf0e6890c1a0 \- GitLab, 访问时间为 二月 6, 2026， [https://code.ornl.gov/llvm-doe/llvm-project/-/blob/16f349251fabacfdba4acac3b25baf0e6890c1a0/mlir/docs/Tutorials/CreatingADialect.md](https://code.ornl.gov/llvm-doe/llvm-project/-/blob/16f349251fabacfdba4acac3b25baf0e6890c1a0/mlir/docs/Tutorials/CreatingADialect.md)  
21. Creating a Dialect \- MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Tutorials/CreatingADialect/](https://mlir.llvm.org/docs/Tutorials/CreatingADialect/)  
22. LLVM Testing Infrastructure Guide — LLVM 23.0.0git documentation, 访问时间为 二月 6, 2026， [https://llvm.org/docs/TestingGuide.html](https://llvm.org/docs/TestingGuide.html)  
23. Test Driven Development Using Google Test \- eInfochips, 访问时间为 二月 6, 2026， [https://www.einfochips.com/blog/test-driven-development-using-google-test/](https://www.einfochips.com/blog/test-driven-development-using-google-test/)  
24. LLVM Alias Analysis Infrastructure — LLVM 23.0.0git documentation, 访问时间为 二月 6, 2026， [https://llvm.org/docs/AliasAnalysis.html](https://llvm.org/docs/AliasAnalysis.html)  
25. Custom Alias Analysis in LLVM | Sy Brand, 访问时间为 二月 6, 2026， [https://tartanllama.xyz/posts/llvm-alias-analysis/](https://tartanllama.xyz/posts/llvm-alias-analysis/)  
26. Interfaces \- MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Interfaces/](https://mlir.llvm.org/docs/Interfaces/)  
27. Interfaces \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Interfaces/\#operation-interfaces](https://mlir.llvm.org/docs/Interfaces/#operation-interfaces)  
28. Traits \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Traits/](https://mlir.llvm.org/docs/Traits/)  
29. MLIR — Using Traits \- Math ∩ Programming, 访问时间为 二月 6, 2026， [https://www.jeremykun.com/2023/09/07/mlir-using-traits/](https://www.jeremykun.com/2023/09/07/mlir-using-traits/)  
30. MLIR Part 4 \- Linear Algebra in MLIR \- Stephen Diehl, 访问时间为 二月 6, 2026， [https://www.stephendiehl.com/posts/mlir\_linear\_algebra/](https://www.stephendiehl.com/posts/mlir_linear_algebra/)  
31. Chapter 0: A Primer on “Structured” Linalg Operations \- MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Tutorials/transform/Ch0/](https://mlir.llvm.org/docs/Tutorials/transform/Ch0/)  
32. Linalg fusion \- MLIR \- LLVM Discussion Forums, 访问时间为 二月 6, 2026， [https://discourse.llvm.org/t/linalg-fusion/824](https://discourse.llvm.org/t/linalg-fusion/824)  
33. 'linalg' Dialect \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Dialects/Linalg/](https://mlir.llvm.org/docs/Dialects/Linalg/)  
34. MLIR — Lowering through LLVM \- Math ∩ Programming, 访问时间为 二月 6, 2026， [https://www.jeremykun.com/2023/11/01/mlir-lowering-through-llvm/](https://www.jeremykun.com/2023/11/01/mlir-lowering-through-llvm/)  
35. Using MLIR from C and Python \- LLVM, 访问时间为 二月 6, 2026， [https://llvm.org/devmtg/2024-10/slides/tutorial/Zinenko-UsingMLIR-from-C-and-Python.pdf](https://llvm.org/devmtg/2024-10/slides/tutorial/Zinenko-UsingMLIR-from-C-and-Python.pdf)  
36. MLIR Python Bindings, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Bindings/Python/](https://mlir.llvm.org/docs/Bindings/Python/)  
37. LLVM Tutorial \- Learn LLVM Step by Step | CompilerSutra, 访问时间为 二月 6, 2026， [https://www.compilersutra.com/docs/llvm/intermediate/backend/](https://www.compilersutra.com/docs/llvm/intermediate/backend/)  
38. Writing an LLVM Backend — LLVM 23.0.0git documentation, 访问时间为 二月 6, 2026， [https://llvm.org/docs/WritingAnLLVMBackend.html](https://llvm.org/docs/WritingAnLLVMBackend.html)  
39. Dialect Conversion \- MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/DialectConversion/](https://mlir.llvm.org/docs/DialectConversion/)  
40. MLIR — Dialect Conversion \- Math ∩ Programming, 访问时间为 二月 6, 2026， [https://www.jeremykun.com/2023/10/23/mlir-dialect-conversion/](https://www.jeremykun.com/2023/10/23/mlir-dialect-conversion/)  
41. Bufferization \- MLIR \- LLVM, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Bufferization/](https://mlir.llvm.org/docs/Bufferization/)  
42. Bufferization \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Bufferization/\#what-is-one-shot-bufferize](https://mlir.llvm.org/docs/Bufferization/#what-is-one-shot-bufferize)  
43. Bufferization \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Bufferization/\#how-to-resolve-conflicts](https://mlir.llvm.org/docs/Bufferization/#how-to-resolve-conflicts)  
44. 'gpu' Dialect \- MLIR, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Dialects/GPU/](https://mlir.llvm.org/docs/Dialects/GPU/)  
45. High-Performance GPU-to-CPU Transpilation and Optimization via High-Level Parallel Constructs \- OSTI, 访问时间为 二月 6, 2026， [https://www.osti.gov/servlets/purl/1959469](https://www.osti.gov/servlets/purl/1959469)  
46. Quickstart tutorial to adding MLIR graph rewrite, 访问时间为 二月 6, 2026， [https://mlir.llvm.org/docs/Tutorials/QuickstartRewrites/](https://mlir.llvm.org/docs/Tutorials/QuickstartRewrites/)  
47. IREE / MLIR / Linalg tutorial \- IREE, 访问时间为 二月 6, 2026， [https://iree.dev/community/blog/2024-01-29-iree-mlir-linalg-tutorial/](https://iree.dev/community/blog/2024-01-29-iree-mlir-linalg-tutorial/)